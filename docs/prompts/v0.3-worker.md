# Worker delegation prompt — v0.3 plugin scaffold

> **Historical/non-operational record.** This dated record is retained for context only. Current guidance: [README.md](../../README.md), [AGENTS.md](../../AGENTS.md), and [plugin README](../../lazybuddy-plugin/README.md).

> Paste the prompt below into a fresh worker session (general-purpose agent).
> This is a **scaffold/creation phase** — the worker builds the plugin shell with
> placeholder components, no runtime behavior yet.

---

## Prompt to delegate

You are working in the LazyBuddy workspace at `/Users/Admin/Desktop/lazybuddy`. This is a WorkBuddy-native recreation of LazyCodex (the OmO agent harness for Codex). The whole project is a **version 0 build** — every phase is numbered `v0.N`. v0.1 (architecture) and v0.2 (project memory) are complete and approved.

**Your task: execute phase v0.3 — build the installable WorkBuddy plugin scaffold.**

This phase creates the `lazybuddy-plugin/` shell: a valid manifest, empty component directories, placeholder commands/skills that say "implemented in later version", an empty hooks scaffold, and validation scripts. No runtime behavior is implemented yet — this is the skeleton that v0.4+ fills in.

### Source material (read before writing anything)

1. `plan/v0.3-plugin-scaffold.md` — the full spec for this phase (read it first; it lists the exact directory structure, manifest fields, placeholder components, and validation scripts required).
2. `docs/lazybuddy-plugin-design.md` — the approved v0.1 plugin design: directory layout, manifest fields, component list, install/uninstall story. Follow this for the manifest schema.
3. `docs/lazybuddy-architecture-plan.md` — the three-layer model. The plugin is the **Plugin layer** (`lazybuddy-plugin/`); it is distinct from the Project Memory layer (`.workbuddy/`, done in v0.2) and the Run State layer (`.lazybuddy/`, v0.7).
4. `docs/lazybuddy-command-index.md` — the master index of commands/skills; your placeholders must match these names.
5. `docs/lazybuddy-parity-ledger.md` — update this after creating the scaffold.
6. `dev/reference/lazycodex/plugins/omo/.codex-plugin/plugin.json` — the real LazyCodex plugin manifest (omo v4.16.0). Use it as the structural model: it has `name`, `version`, `description`, `skills`, `hooks` (array), `mcpServers`, `interface` block. Mirror this shape for LazyBuddy.
7. `dev/reference/lazycodex/plugins/omo/README.md` — how LazyCodex documents its plugin.

### Verified WorkBuddy facts to ground your work in

These were confirmed against the official CodeBuddy plugin reference — do not invent alternatives:
- Manifest: `.codebuddy-plugin/plugin.json` (only `name` is required; include metadata fields for quality)
- **Components live at the plugin root, NOT inside `.codebuddy-plugin/`** — only `plugin.json` goes in `.codebuddy-plugin/`
- Skills: `skills/<name>/SKILL.md` · Commands: `commands/*.md` · Agents: `agents/*.md`
- Hooks: `hooks/hooks.json` · MCP: `.mcp.json` · LSP: `.lsp.json` (optional)
- Env vars: `${CODEBUDDY_PLUGIN_ROOT}` (plugin install dir) and `${CODEBUDDY_PLUGIN_DATA}` (persistent state) — use these in scripts and configs, never ad-hoc names
- Keep run-specific state in `.lazybuddy/` (project-level), NOT in plugin data — plugin data is for installed deps/caches only

### What to produce

**1. Plugin manifest**
- `lazybuddy-plugin/.codebuddy-plugin/plugin.json` — fields: `name: "lazybuddy"`, `version: "0.3.0"` (tracks the phase), `description`, `author` (placeholder), `skills: "./skills/"`, `commands: "./commands/"`, `agents: "./agents/"`, `hooks: "./hooks/hooks.json"`, `mcpServers: "./.mcp.json"`, plus an `interface` block (displayName, shortDescription, category, capabilities) mirroring the LazyCodex omo manifest.

**2. Plugin docs**
- `lazybuddy-plugin/README.md` — what the plugin is, install/uninstall story, component map, status (scaffold — components implemented in later versions).
- `lazybuddy-plugin/CHANGELOG.md` — start with v0.3.0 entry.

**3. Placeholder commands** (`lazybuddy-plugin/commands/`)
Create these 8 files. Each is a minimal markdown stub with frontmatter/description stating it is a placeholder "implemented in v0.4+":
`init-deep.md`, `ulw-plan.md`, `start-work.md`, `ulw-loop.md`, `verifier.md`, `reviewer.md`, `librarian.md`, `migration-planner.md`

**4. Placeholder skills** (`lazybuddy-plugin/skills/<name>/SKILL.md`)
Create these 8 skill dirs, each with a `SKILL.md` stub (YAML frontmatter `name` + `description`, body says "Placeholder — implemented in v0.4+"):
`init-deep`, `ulw-plan`, `start-work`, `ulw-loop`, `verifier`, `reviewer`, `librarian`, `migration-planner`

**5. Empty component dirs** (with `.gitkeep`)
- `lazybuddy-plugin/agents/.gitkeep`
- `lazybuddy-plugin/mcp/.gitkeep`
- `lazybuddy-plugin/scripts/.gitkeep`
- `lazybuddy-plugin/schemas/.gitkeep`
- `lazybuddy-plugin/tests/.gitkeep`
- `lazybuddy-plugin/docs/.gitkeep`

**6. Hooks + MCP scaffolds**
- `lazybuddy-plugin/hooks/hooks.json` — valid JSON with the 12 event types as empty arrays (`SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PreCompact`, `Stop`, `StopFailure`, `TaskCreated`, `TaskCompleted`, `SubagentStart`, `SubagentStop`). No hook logic yet — just the structure.
- `lazybuddy-plugin/.mcp.json` — valid JSON with empty `mcpServers: {}` (servers added in v0.8).

**7. Validation scripts** (`lazybuddy-plugin/scripts/`)
Create these 4 scripts (use `.sh` for shell, make them executable):
- `lazybuddy-plugin-doctor.sh` — validates plugin structure: manifest exists + parses as JSON, all component dirs exist, all placeholder skills/commands present.
- `lazybuddy-smoke-test.sh` — checks each SKILL.md has valid YAML frontmatter, each command file is non-empty markdown.
- `lazybuddy-docs-check.sh` — checks no broken internal markdown links in the plugin dir.
- `lazybuddy-parity-check.sh` — stub that will compare plugin structure against `dev/reference/lazycodex/plugins/omo/` (just echoes "parity check — implemented in later version" for now).
Use `${CODEBUDDY_PLUGIN_ROOT}` for any self-referencing paths inside these scripts.

### Hard constraints

- **No runtime behavior.** Every placeholder must clearly say "implemented in v0.4+" or equivalent. Do not write skill logic, agent definitions, or hook scripts.
- **Components at plugin root, NOT inside `.codebuddy-plugin/`.** Only `plugin.json` goes in `.codebuddy-plugin/`.
- **`plugin.json` must be valid JSON** — verify by parsing.
- **`hooks.json` and `.mcp.json` must be valid JSON.**
- **Use `${CODEBUDDY_PLUGIN_ROOT}` / `${CODEBUDDY_PLUGIN_DATA}`** — never ad-hoc env-var names.
- **Keep run state out of plugin data** — `.lazybuddy/` is for run state; `${CODEBUDDY_PLUGIN_DATA}` is for installed deps/caches only.
- **Match the LazyCodex omo manifest structure** (traced to `dev/reference/lazycodex/plugins/omo/.codex-plugin/plugin.json`) — same field shapes, adapted for WorkBuddy.

### Verification (perform before reporting done)

1. `plugin.json` parses as valid JSON and has `name`, `version`, `skills`, `commands`, `agents`, `hooks`, `mcpServers` fields.
2. All 8 placeholder commands exist under `commands/`.
3. All 8 placeholder skills exist as `skills/<name>/SKILL.md` with valid YAML frontmatter.
4. All empty dirs have `.gitkeep`.
5. `hooks.json` and `.mcp.json` parse as valid JSON.
6. `lazybuddy-plugin-doctor.sh` runs and reports all checks PASS.
7. `lazybuddy-smoke-test.sh` runs and reports all skills/commands valid.
8. No placeholder claims runtime behavior it doesn't have.
9. Update `docs/lazybuddy-parity-ledger.md` — note the plugin scaffold now exists (status shifts from designed to scaffolded).

### Required final response format

End your work with a report containing exactly these sections:
- **What I inspected** (file paths read, in this repo, in docs/, and in dev/reference/lazycodex/)
- **What I found** (key decisions: manifest fields chosen, how you structured the hooks scaffold, which env vars you used)
- **What I changed** (every file/dir created, with one-line summary each)
- **How to run it** (the exact commands to run the doctor + smoke-test scripts)
- **Verification performed** (the 9 checks above, each PASS/FAIL with evidence)
- **Remaining gaps** (anything unresolved for v0.4)
- **Next prompt to paste** (the v0.4 delegation prompt, ready to use)
