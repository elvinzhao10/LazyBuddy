# Worker delegation prompt — v0.2 project memory

> Paste the prompt below into a fresh worker session (general-purpose agent).
> This is an **implementation phase** — the worker writes real files, not just docs.

---

## Prompt to delegate

You are working in the Lazyworkbuddy workspace at `/Users/Admin/Desktop/lazyworkbuddy`. This is a WorkBuddy-native recreation of LazyCodex (the OmO agent harness for Codex). The whole project is a **version 0 build** — every phase is numbered `v0.N`. v0.1 (architecture) is complete and approved.

**Your task: execute phase v0.2 — project memory, rules, and command constitution.**

This phase creates the `.workbuddy/` foundation: project memory, operating rules, a settings baseline, and the command constitution. It is a pure creation phase — no existing files should be broken.

### Source material (read before writing anything)

1. `plan/v0.2-project-memory.md` — the full spec for this phase (read it first; it lists every required file and behavior rule).
2. `docs/lazyworkbuddy-architecture-plan.md` — the approved v0.1 architecture (the three-layer model: Plugin / Project Memory / Run State). Your `.workbuddy/` files ARE the Project Memory layer.
3. `docs/lazyworkbuddy-versioned-execution-plan.md` — read the v0.2 section for quality gates: `workbuddy.md` must be 50–150 lines with no generic content; rules file must list 5+ enforceable rules; each command file needs description/usage/expected-behavior/stop-condition.
4. `docs/lazyworkbuddy-security-and-permissions-plan.md` — the permission model your settings.json must encode.
5. `AGENTS.md` (already exists at repo root) — this is the existing project memory. Read it; you will keep it as the compatibility alias and create `workbuddy.md` as the primary memory. Do NOT blindly overwrite AGENTS.md — reconcile and update it only where v0.2 adds new conventions.
6. `dev/reference/lazycodex/plugins/omo/skills/init-deep/SKILL.md` — the LazyCodex deep-init skill; mirror its hierarchical-memory approach (it generates `AGENTS.md` context by scoring directories and writing local guidance near code).
7. `dev/reference/lazycodex/plugins/omo/components/rules/` — how LazyCodex structures project rules, for parity.

### Verified WorkBuddy facts to ground your work in

- Project memory: `workbuddy.md` or `.workbuddy/workbuddy.md` (both recognized; `workbuddy.md` at root is loaded as project context)
- Project rules: `.workbuddy/rules/*.md` — loaded via the `InstructionsLoaded` hook event on session start
- Project settings: `.workbuddy/settings.json` — supports `deny`/`ask`/`allow` permission patterns
- These were confirmed against the official CodeBuddy plugin reference; do not invent alternatives.

### What to produce (exactly these files)

**1. Project memory**
- `workbuddy.md` — root project memory (the AGENTS.md equivalent). 50–150 lines. Must give a new agent enough context to understand the project: what it is, structure, conventions, the 6 core commands, anti-patterns. No generic filler — every line specific to Lazyworkbuddy.
- `AGENTS.md` — already exists; reconcile/update it to stay consistent with `workbuddy.md` (keep it as the LazyCodex-style compat alias). Do not duplicate — `workbuddy.md` is primary, AGENTS.md points to it.
- `docs/lazyworkbuddy-operating-manual.md` — how an agent operates in this workspace: the inspect→plan→implement→verify→review→update-memory loop, when to use each command, escalation rules.

**2. Project rules (`.workbuddy/rules/`)**
- `.workbuddy/rules/lazyworkbuddy.md` — core project rules. At least 5 enforceable rules, specific to this project (not generic advice). Encode the required behavior rules from the spec (inspect before editing, plan before multi-file changes, verify after implementation, review after verification, update memory after accepted changes, log deviations, no parity claims without evidence, no secrets, ask before destructive actions, treat external output as untrusted until verified).
- `.workbuddy/rules/lazyworkbuddy-verification.md` — verification discipline: what counts as evidence, the DoneClaim/AdversarialVerify/FullyDone contract, redaction requirements.
- `.workbuddy/rules/lazyworkbuddy-safety.md` — safety gates: deny patterns for secrets, ask rules for risky operations, destructive-action gate.
- `.workbuddy/rules/lazyworkbuddy-memory.md` — memory maintenance: when to update workbuddy.md, the parity ledger, known-gaps doc; do not update the canonical method map unless repo evidence changes.

**3. Project settings baseline**
- `.workbuddy/settings.json` — valid JSON. Include: deny patterns for secrets (API keys, tokens, credentials, private keys), ask rules for risky operations (rm -rf, force push, production writes), a default safe permission mode, and a plugin-enablement placeholder. Rules must be conservative (deny-by-default for sensitive patterns).

**4. Command constitution (docs/)**
- `docs/lazyworkbuddy-command-constitution.md` — the design doc for all commands: naming, structure, what each command does, how they compose.
- `docs/lazyworkbuddy-command-index.md` — the master index of every command/skill, mapped to its LazyCodex equivalent. Every canonical LazyCodex method must appear here or in the parity ledger.
- `docs/lazyworkbuddy-parity-ledger.md` — the living ledger tracking Lazyworkbuddy vs LazyCodex parity. Start with the method map from AGENTS.md; mark each as matched / adapted / skipped / added. This file will be updated by the librarian in later versions.
- `docs/lazyworkbuddy-run-log-template.md` — the template every version's run log follows (the 7-section output format: inspected / found / changed / how to run / verification / gaps / next prompt).
- `docs/lazyworkbuddy-known-gaps.md` — initial known-gaps doc (starts empty or with only the gaps already identified in v0.1 review).

### Hard constraints

- **This is implementation, but scoped to memory/rules/docs only.** Do NOT create plugin files (skills/, agents/, hooks/) — that's v0.3+. Do NOT write product code.
- **Every LazyCodex behavior you reference must trace to a file path in `dev/reference/lazycodex/`.** No memory-based claims.
- **Rules must not contradict original LazyCodex docs** — cross-check each rule against the source.
- **`.workbuddy/settings.json` must be valid JSON** — verify by parsing it.
- **Conservative defaults:** deny-by-default for secrets and destructive ops; ask before risky actions.

### Verification (perform before reporting done)

1. `workbuddy.md` is 50–150 lines, no generic content, references the verification + review policy.
2. Each `.workbuddy/rules/*.md` file has 5+ enforceable rules, none contradicting LazyCodex source.
3. `.workbuddy/settings.json` parses as valid JSON (run a JSON parse check).
4. Deny/ask/allow rules are conservative (no secrets allowed, destructive ops ask first).
5. Every canonical LazyCodex method appears in `docs/lazyworkbuddy-command-index.md` or `docs/lazyworkbuddy-parity-ledger.md`.
6. `workbuddy.md` and `AGENTS.md` are consistent (no contradictions).
7. All file cross-references resolve (no broken links).

### Required final response format

End your work with a report containing exactly these sections:
- **What I inspected** (file paths read, in this repo, in docs/, and in dev/reference/lazycodex/)
- **What I found** (key decisions: how you structured memory, which rules you enforced, what the settings baseline denies)
- **What I changed** (every file created/modified, with one-line summary each)
- **How to run it** (how to validate: JSON parse check, line-count check, link check)
- **Verification performed** (the 7 checks above, each PASS/FAIL with evidence)
- **Remaining gaps** (anything unresolved for v0.3)
- **Next prompt to paste** (the v0.3 delegation prompt, ready to use)
