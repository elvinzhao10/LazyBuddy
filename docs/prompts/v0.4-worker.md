# Worker delegation prompt — v0.4 skills & commands

> Paste the prompt below into a fresh worker session (general-purpose agent).  
> This is the **core implementation phase** — the worker replaces v0.3 placeholders  
> with real skill workflow logic, porting LazyCodex semantics to WorkBuddy-native format.

---

## Prompt to delegate

You are working in the LazyBuddy workspace at `/Users/Admin/Desktop/lazybuddy`. This is a WorkBuddy-native recreation of LazyCodex (the OmO agent harness for Codex). The whole project is a **version 0 build** — every phase is numbered `v0.N`. v0.1 (architecture), v0.2 (project memory), and v0.3 (plugin scaffold) are complete and approved.

**Your task: execute phase v0.4 — implement LazyCodex-style Skills and slash commands.**

This phase replaces the v0.3 placeholder stubs with real workflow logic. The 8 scaffolded skills/commands (init-deep, ulw-plan, start-work, ulw-loop, verifier, reviewer, librarian, migration-planner) become functional, plus you port the key LazyCodex workflow skills (ultrawork, review-work, programming, remove-ai-slops, git-master, debugging) that the architecture calls for.

### Source material (read before writing anything)

1. `plan/v0.4-skills-commands.md` — the full spec for this phase (read it first; it lists every required skill, its behavior details, and the per-skill/per-command requirements).
2. `docs/lazybuddy-versioned-execution-plan.md` — read the v0.4 section for the expanded skill list (10+ skills) and the tool-translation guidance.
3. `docs/lazybuddy-command-constitution.md` — the command design doc (naming, structure, composition).
4. `docs/lazybuddy-command-index.md` — the master index; your implementations must match these names.
5. `docs/lazybuddy-state-ledger-design.md` — the `.lazybuddy/runs/<run_id>/` schema that start-work, ulw-loop, and ulw-plan write to.
6. `.workbuddy/rules/lazybuddy-verification.md` — the verification discipline your skills must enforce (DoneClaim/AdversarialVerify/FullyDone, 5 gates, redaction).
7. **The canonical LazyCodex source skills** — read EACH of these before porting. These are the source of truth for semantics; you adapt, not invent:
   - `dev/reference/lazycodex/plugins/omo/skills/init-deep/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/ulw-plan/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/start-work/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/ulw-loop/SKILL.md` (and `references/full-workflow.md` in that dir)
   - `dev/reference/lazycodex/plugins/omo/skills/ultrawork/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/review-work/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/programming/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/remove-ai-slops/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/git-master/SKILL.md`
   - `dev/reference/lazycodex/plugins/omo/skills/debugging/SKILL.md`

### Tool translation (critical — do this for every skill)

LazyCodex skills reference Codex-specific tools and paths. You MUST translate these to WorkBuddy-native equivalents:

| LazyCodex (Codex)                 | LazyBuddy (WorkBuddy)                                         |
| --------------------------------- | ----------------------------------------------------------------- |
| `multi_agent_v1.spawn_agent(...)` | WorkBuddy `Agent` tool / subagent spawning                        |
| `multi_agent_v1.wait_agent(...)`  | WorkBuddy subagent wait/poll                                      |
| `fork_context: false`             | agent `isolation: true` (no parent history)                       |
| `agent_type: "explorer"` etc.     | pass role name in agent `message` + matching `agents/*.md` (v0.5) |
| `.omo/boulder.json`               | `.lazybuddy/runs/<run_id>/state.json`                         |
| `.omo/start-work/ledger.jsonl`    | `.lazybuddy/runs/<run_id>/events.jsonl`                       |
| `.omo/plans/<slug>.md`            | `.lazybuddy/plans/<slug>.md`                                  |
| `.omo/evidence/`                  | `.lazybuddy/runs/<run_id>/evidence/`                          |
| `omo ulw-loop create-goals` (CLI) | MCP tool calls or inline skill logic (no CLI yet)                 |
| `${PLUGIN_ROOT}`                  | `${CODEBUDDY_PLUGIN_ROOT}`                                        |

Preserve all method semantics: tier triage (LIGHT/HEAVY), evidence gates, delegate-only orchestration, the PIN→RED→GREEN→SURFACE→CLEAN loop, the 5-agent review lanes, the Sisyphus completion contract (DoneClaim→AdversarialVerify→FullyDone).

### What to produce

**1. Replace the 8 scaffolded skills** (`lazybuddy-plugin/skills/<name>/SKILL.md`) with real content:

- `init-deep` — inspect repo structure, identify language/runtime/commands/tests, generate `.lazybuddy/context/{index.md,commands.json,project-map.json}`. Read-only; never modify product code.
- `ulw-plan` — read-only planning (Prometheus). Explore, ask only blocking questions, write plan to `.lazybuddy/plans/<slug>.md`. Never implement.
- `start-work` — load plan + state, select next bounded task, delegate implementation to subagents (never implement directly), update run log, invoke verifier before marking done. Orchestrator-only.
- `ulw-loop` — continue bounded work cycles; after each cycle: verify, review, update memory, checkpoint. Stop only when complete/blocked/budget-exceeded.
- `verifier` — discover available checks, run them, summarize exact commands + results, distinguish pass/fail/warning/skipped/N-A. Independent from executor.
- `reviewer` — review changed files against intent + LazyCodex parity, check overreach/missing tests/missing docs, produce accept/reject/revise.
- `librarian` — update memory after accepted changes (workbuddy.md, command index, parity ledger, known gaps, risk register). Never rewrite canonical method map unless repo evidence changes.
- `migration-planner` — create host-adapter plans for future platforms. Require canonical repo inspection + semantic mapping.

**2. Port the additional LazyCodex workflow skills** (create new `skills/<name>/SKILL.md`):

- `ultrawork` — binding ultrawork directive, tier triage (LIGHT/HEAVY), PIN→RED→GREEN→SURFACE→CLEAN loop, binding reviewer gate.
- `review-work` — 5-agent parallel review (correctness, security, performance, maintainability, parity). All lanes must PASS.
- `programming` — strict coding discipline (TypeScript/Rust/Python/Go).
- `remove-ai-slops` — behavior-preserving cleanup of AI-looking code.
- `git-master` — git workflow discipline.
- `debugging` — systematic debugging (hypothesis → check → confirm root cause → minimal failing test → fix).

**3. Replace the 8 scaffolded commands** (`lazybuddy-plugin/commands/*.md`) with real content. Each command must have: concise invocation, inputs, outputs, success criteria, link to its Skill, link to the canonical method map, and "do not claim completion without verification."

**4. Update docs:**

- `docs/lazybuddy-command-index.md` — mark the implemented skills/commands as active (not placeholder).
- `docs/lazybuddy-parity-ledger.md` — update status for each ported skill (adapted/matched).
- `docs/lazybuddy-verification-matrix.md` — add verification entries for each skill.

### Per-skill required structure

Every SKILL.md must have:

- YAML frontmatter: `name`, `description` (with trigger keywords)
- **LazyCodex source:** exact file path in `dev/reference/lazycodex/`
- **Purpose:** what this skill does in LazyBuddy
- **Trigger conditions:** when to invoke
- **Required context:** what to inspect before acting
- **Tool access:** what tools this skill assumes
- **Step-by-step procedure:** the workflow
- **Expected output artifacts:** what files/state it produces
- **Verification gates:** how completion is verified
- **Failure behavior:** what happens on failure
- **Handoff format:** how it passes control to the next workflow
- **WorkBuddy-native features:** how it uses Skills/Agents/Hooks/MCP/Plan Mode

### Hard constraints

- **Preserve LazyCodex semantics.** This is a clean-room adaptation — same behavior, WorkBuddy-native surfaces. Do not copy source text; reimplement.
- **Every LazyCodex behavior claim must trace to a file path in `dev/reference/lazycodex/`.**
- **Replace Codex-specific tools** with WorkBuddy equivalents (see translation table above). No `multi_agent_v1`, no `.omo/` paths, no `${PLUGIN_ROOT}`.
- **Orchestrator never implements directly.** start-work delegates to subagents; it never writes product code.
- **No completion without verification.** Every skill that marks work done must require evidence.
- **Record all deviations** from LazyCodex in `docs/lazybuddy-known-gaps.md`.
- **Valid YAML frontmatter** on every SKILL.md.

### Verification (perform before reporting done)

1. Run `bash lazybuddy-plugin/scripts/lazybuddy-plugin-doctor.sh` — must still PASS.
2. Run `bash lazybuddy-plugin/scripts/lazybuddy-smoke-test.sh` — must PASS for all skills (frontmatter + name + description).
3. Every SKILL.md traces its LazyCodex source to a real file path.
4. No remaining Codex-specific references (`multi_agent_v1`, `.omo/`, `${PLUGIN_ROOT}`) in any skill.
5. Every skill has all 12 required structure sections.
6. Every command links to its Skill and the method map.
7. `docs/lazybuddy-known-gaps.md` records any semantic deviations.
8. Parity ledger updated for each ported skill.

### Required final response format

End your work with a report containing exactly these sections:

- **Skill inventory table** (name | LazyCodex source | status | lines)
- **Command inventory table** (name | links to skill | status)
- **What I inspected** (file paths read in this repo and dev/reference/lazycodex/)
- **What I found** (key adaptation decisions, tool translations made, semantic deviations)
- **What I changed** (every file created/modified, with one-line summary)
- **How to run it** (doctor + smoke-test commands)
- **Verification performed** (the 8 checks above, each PASS/FAIL with evidence)
- **Remaining gaps** (anything unresolved for v0.5)
- **Next prompt to paste** (the v0.5 delegation prompt, ready to use)
