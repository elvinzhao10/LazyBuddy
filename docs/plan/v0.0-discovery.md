# LazyBuddy v0.0 — repo discovery and workbuddy capability extraction

> **Historical/non-operational record.** This dated record is retained for context only. Current guidance: [README.md](../../README.md), [AGENTS.md](../../AGENTS.md), and [plugin README](../../lazybuddy-plugin/README.md).

LazyBuddy v0.0 — repo discovery and workbuddy capability extraction

LazyBuddy v0.0 — Discover the original LazyCodex contract and the workbuddy host surface.
You are LazyBuddy. Your task is to recreate LazyCodex inside this workbuddy workspace using workbuddy-native capabilities.
The LazyCodex repo is already present in this workspace. Treat it as canonical.
Do not implement runtime behavior yet.
Your job in this phase:
1. Locate the LazyCodex repo root.
2. Inspect all top-level docs, command files, skill files, agent prompts, scripts, tests, package manifests, config files, examples, and plugin metadata.
3. Identify the project language, package manager, runtime requirements, entrypoints, commands, scripts, tests, and configuration files.
4. Extract the complete LazyCodex method list.
5. Identify the original LazyCodex workflow lifecycle:
   - deep initialization
   - project memory creation/update
   - planning
   - autonomous work start
   - iterative work loop
   - verification
   - review
   - librarian/memory update
   - migration planning
   - hooks
   - MCP/tool integration
   - subagent/team behavior
6. Determine exact command names from the repo. Do not assume names.
7. Identify which behaviors are core LazyCodex semantics versus host-specific implementation details.
8. Inspect this workbuddy workspace for available native surfaces:
   - workbuddy.md
   - .workbuddy/
   - .workbuddy/settings.json
   - .workbuddy/rules/
   - .workbuddy/agents/
   - .workbuddy/skills/
   - .workbuddy/commands/
   - existing plugin files
   - existing MCP configs
   - existing hooks
   - package scripts
   - test commands
9. Create a migration map from LazyCodex to LazyBuddy.
Required output files:
- docs/lazycodex-canonical-method-map.md
- docs/lazybuddy-official-capability-map.md
- docs/lazybuddy-host-adaptation-map.md
- docs/lazybuddy-v0.0-discovery-report.md
For every LazyCodex method, document:
- original name
- source file/path
- purpose
- inputs
- outputs
- side effects
- dependencies
- verification expectations
- LazyBuddy equivalent
- implementation status:
  - discovered
  - mapped
  - unknown
  - not applicable
For every workbuddy host surface, document:
- surface name
- local path or configuration location
- purpose
- whether it will be used
- how it maps to LazyCodex behavior
- risks or limitations
Output requirements:
- Be specific.
- Reference actual files and paths.
- Mark unknowns clearly.
- Do not guess LazyCodex behavior from memory.
- Do not make implementation changes beyond documentation in this phase.
Verification:
- Confirm every top-level LazyCodex directory was inspected or explicitly skipped with reason.
- Confirm every discovered method has at least one source path.
- Confirm every proposed LazyBuddy equivalent has a host-native implementation path.
- Run existing repo discovery commands only if safe, such as:
  - find
  - ls
  - tree, if available
  - package-manager script listing
  - grep/ripgrep
- Do not run install, build, or mutation commands unless already known safe.
Required final response:
- What I inspected
- What I found
- What I changed
- How to run it
- Verification performed
- Remaining gaps
- Next prompt to paste
