# LazyBuddy v0.3 — plugin scaffold

LazyBuddy v0.3 — plugin scaffold

LazyBuddy v0.3 — Build the installable workbuddy plugin scaffold.
Use:
- docs/lazybuddy-architecture-plan.md
- docs/lazybuddy-plugin-design.md
- docs/lazybuddy-command-index.md
- docs/lazybuddy-parity-ledger.md
Your task:
Create the workbuddy plugin shell for LazyBuddy.
Design:
The repo-local .workbuddy layer provides project memory and rules.
The plugin provides reusable installable behavior:
- skills
- commands
- agents
- hooks
- MCP servers
- scripts
- optional LSP/tool configs
Required structure:
lazybuddy-plugin/
  .codebuddy-plugin/
    plugin.json
  README.md
  CHANGELOG.md
  commands/
  skills/
  agents/
  hooks/
    hooks.json
  mcp/
  scripts/
  schemas/
  tests/
  docs/
  .mcp.json
If the project prefers plugin-at-root instead of nested plugin directory, document the choice and ensure workbuddy plugin discovery works.
Create plugin manifest:
- .codebuddy-plugin/plugin.json
Manifest should include:
- name: lazybuddy
- version: 0.1.0
- description
- author placeholder if unknown
- commands path if non-default
- skills path if non-default
- agents path if non-default
- hooks path if non-default
- mcpServers or .mcp.json reference if appropriate
Important:
- Components must live at the plugin root, not inside .codebuddy-plugin/.
- Use ${CODEBUDDY_PLUGIN_ROOT} for plugin-local scripts.
- Use ${CODEBUDDY_PLUGIN_DATA} for persistent plugin state, caches, installed dependencies, or generated durable plugin data.
- Do not put run-specific workspace state only in plugin data. Keep project run state in .lazybuddy/ so it is inspectable and portable.
Create placeholder component directories:
- commands/init-deep.md
- commands/ulw-plan.md
- commands/start-work.md
- commands/ulw-loop.md
- commands/verifier.md
- commands/reviewer.md
- commands/librarian.md
- commands/migration-planner.md
- skills/init-deep/SKILL.md
- skills/ulw-plan/SKILL.md
- skills/start-work/SKILL.md
- skills/ulw-loop/SKILL.md
- skills/verifier/SKILL.md
- skills/reviewer/SKILL.md
- skills/librarian/SKILL.md
- skills/migration-planner/SKILL.md
- agents/.gitkeep
- hooks/hooks.json
- mcp/.gitkeep
- scripts/.gitkeep
- schemas/.gitkeep
- tests/.gitkeep
Create validation scripts:
- scripts/lazybuddy-plugin-doctor.*
- scripts/lazybuddy-smoke-test.*
- scripts/lazybuddy-docs-check.*
- scripts/lazybuddy-parity-check.*
Verification:
- Validate plugin manifest syntax.
- Run workbuddy plugin validation command if available.
- Run workbuddy --debug or equivalent plugin-load debug command if safe.
- Confirm components are at the correct paths.
- Confirm placeholder commands/skills appear in the expected discovery paths.
- Confirm scripts are executable where needed.
- Update docs/lazybuddy-parity-ledger.md.
Success criteria:
- LazyBuddy has an installable or loadable workbuddy plugin skeleton.
- Plugin structure matches workbuddy conventions.
- No runtime behavior is overclaimed.
- All placeholder components clearly say “implemented in later version” if incomplete.
Required output:
- What I inspected
- What I found
- What I changed
- How to run it
- Verification performed
- Remaining gaps
- Next prompt to paste
