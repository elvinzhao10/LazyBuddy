# Worker delegation prompt — v0.9 verifier/reviewer/librarian hardening

> Paste the prompt below into a fresh worker session (general-purpose agent).
> This phase hardens the quality-control agents and addresses the semantic gaps
> documented in the v0.4 review (G-007 through G-015).

---

## Prompt to delegate

You are working in the Lazyworkbuddy workspace at `/Users/Admin/Desktop/lazyworkbuddy`. This is a WorkBuddy-native recreation of LazyCodex (the OmO agent harness for Codex). The whole project is a **version 0 build** — every phase is numbered `v0.N`. v0.1–v0.8 are complete and approved. The MVP core (v0.0–v0.7) is done; v0.8 added MCP servers.

**Your task: execute phase v0.9 — harden verifier, reviewer, and librarian workflows.**

The quality-control agents exist (v0.4/v0.5) but are not robust enough to evaluate real implementation work. This phase makes them rigorous: the verifier discovers and runs real checks, the reviewer enforces semantic parity, and the librarian maintains all memory artifacts traceably. It also closes the known semantic gaps from the v0.4 review.

### Source material (read before writing anything)

1. `plan/v0.9-hardening.md` — the full spec (read it first; it lists the verifier/reviewer/librarian hardening requirements, 4 check scripts, 4 protocol docs, and 12 quality gates).
2. `docs/lazyworkbuddy-known-gaps.md` — **critical: contains 9 gaps (G-007 through G-015) that target v0.9.** Read each gap and address it. Mark each as resolved in the known-gaps doc.
3. `docs/lazyworkbuddy-verification-matrix.md` — the verification entries your verifier must run.
4. `docs/lazyworkbuddy-parity-ledger.md` — the parity data your reviewer checks and your librarian updates.
5. `.workbuddy/rules/lazyworkbuddy-verification.md` — the verification discipline (DoneClaim/AdversarialVerify/FullyDone, 5 gates, redaction).
6. The existing v0.4/v0.5 implementations to harden:
   - `lazyworkbuddy-plugin/skills/verifier/SKILL.md` + `agents/lazyworkbuddy-verifier.md`
   - `lazyworkbuddy-plugin/skills/reviewer/SKILL.md` + `agents/lazyworkbuddy-reviewer.md`
   - `lazyworkbuddy-plugin/skills/librarian/SKILL.md` + `agents/lazyworkbuddy-librarian.md`
7. `lazyworkbuddy-plugin/scripts/lazyworkbuddy-docs-check.sh` and `lazyworkbuddy-parity-check.sh` — existing stubs to fill with real logic.
8. **LazyCodex sources for the gaps:**
   - `reference/lazycodex/plugins/omo/skills/start-work/SKILL.md` — worktree discipline (lines 71-92), Sisyphus JSON schema (lines 136-160), debugging audit (lines 176-184)
   - `reference/lazycodex/plugins/omo/skills/ulw-loop/references/full-workflow.md` — iteration caps (line 144: 5 cycles/goal, 3 same-criterion failures), dynamic steering (lines 206-220), final quality gate (lines 183-204)
   - `reference/lazycodex/plugins/omo/skills/ultrawork/SKILL.md` — subagent transition barriers (lines 291-302), GREEN PR refresh (lines 226-230)

### Known gaps to resolve (G-007 through G-015)

Read `docs/lazyworkbuddy-known-gaps.md` for full details. Summary of what to fix:

| Gap | Issue | Fix |
| --- | --- | --- |
| G-007 | start-work 9 adversarial classes | Already fixed in v0.4 review — verify still present, mark resolved |
| G-008 | start-work drops worktree discipline | Add worktree requirements to start-work SKILL.md (Phase 2: --worktree for PR/branch, worktree_path in state.json) |
| G-009 | start-work drops debugging runtime audit | Add debugging audit to start-work completion phase (3+ failure hypotheses, distinguishing checks) |
| G-010 | start-work drops Sisyphus JSON schema detail | Add full DoneClaim/AdversarialVerify JSON schema to start-work SKILL.md |
| G-011 | ulw-loop iteration cap discrepancy | Replace 500/100 total-iteration caps with source's 5-cycles-per-goal + 3-same-criterion-failures |
| G-012 | ulw-loop drops dynamic steering + final quality gate + delegation model | Add 7 steering types, final quality gate, and delegation model to ulw-loop SKILL.md |
| G-013 | ultrawork drops subagent transition barriers | Add 4 transition barrier rules to ultrawork SKILL.md |
| G-014 | ultrawork drops GREEN PR refresh + commits | Add GREEN-step PR refresh guidance and commits section to ultrawork SKILL.md |
| G-015 | review-work Context Miner has no agent | Create `lazyworkbuddy-context-miner.md` agent OR document that the lane reuses explorer |

After fixing each gap, mark it as **resolved** in `docs/lazyworkbuddy-known-gaps.md` with the resolution date and what was done.

### What to produce

**1. Harden verifier** (skill + agent + script):
- `skills/verifier/SKILL.md` — add: discovers available test commands (package.json scripts, Makefile targets, CI config); classifies checks into 9 categories (syntax, typecheck, lint, unit, integration, plugin validation, docs consistency, parity coverage, security); runs relevant checks; distinguishes hard failure / soft warning / skipped / N-A; writes summary to `.lazyworkbuddy/runs/<run_id>/verification/`; creates repair tasks for failures via `create-repair-task.sh`.
- `agents/lazyworkbuddy-verifier.md` — strengthen evidence-checking rules: adversarial class probing (stale_state, dirty_worktree, misleading_success_output), reproducibility requirement, confidence scoring (confirmed requires ≥0.8).
- `scripts/lazyworkbuddy-verify.sh` — **new**: the actual verification runner. Discovers checks, runs them, outputs compact JSON summary. Calls doctor.sh, smoke-test.sh, docs-check.sh, parity-check.sh, security-check.sh as sub-checks.

**2. Harden reviewer** (skill + agent):
- `skills/reviewer/SKILL.md` — add: reviews changed files against original intent; checks for overreach (scope creep), missing docs, missing tests, regressions, LazyCodex semantic preservation, WorkBuddy adaptation justification; produces accept/reject/revise; writes to `.lazyworkbuddy/runs/<run_id>/review/`.
- `agents/lazyworkbuddy-reviewer.md` — add cross-lane consistency (if reviewer finds a bug verifier missed → flag), retry budget tracking (max 3 retries per lane), lane completion order.

**3. Harden librarian** (skill + agent):
- `skills/librarian/SKILL.md` — add: after accepted changes updates workbuddy.md, command index, parity ledger, known gaps, risk register, operating manual; never rewrites canonical method map unless repo evidence changes; writes to `.lazyworkbuddy/runs/<run_id>/memory_updates/`.
- `agents/lazyworkbuddy-librarian.md` — strengthen: diff-before-write (only append new sections, never delete human content), traceable updates (every change logged with source + timestamp).

**4. Fill check scripts** (all executable):
- `scripts/lazyworkbuddy-verify.sh` — **new**: runs all verification checks, outputs compact JSON.
- `scripts/lazyworkbuddy-docs-check.sh` — **fill existing stub**: check all internal markdown links resolve, check no broken cross-references.
- `scripts/lazyworkbuddy-parity-check.sh` — **fill existing stub**: compare plugin structure against `reference/lazycodex/plugins/omo/`, report coverage.
- `scripts/lazyworkbuddy-security-check.sh` — **new**: scan all files for secret patterns (API keys, tokens, private keys), report any found.

**5. Create 4 protocol docs:**
- `docs/lazyworkbuddy-verifier-protocol.md` — the 9 check categories, how each is discovered and run, pass/fail criteria, repair task creation.
- `docs/lazyworkbuddy-reviewer-protocol.md` — the review dimensions (intent, scope, docs, tests, regressions, parity, adaptation), accept/reject/revise decision tree.
- `docs/lazyworkbuddy-librarian-protocol.md` — what gets updated when, diff-before-write rule, traceability format.
- `docs/lazyworkbuddy-quality-gates.md` — the 12 quality gates (from spec lines 70-82): method_map_complete, command_index_complete, skill_command_pairing, agent_boundary_clear, hook_config_valid, state_schema_valid, docs_links_valid, plugin_manifest_valid, no_secret_patterns, verification_recorded, reviewer_decision_recorded, librarian_update_recorded, final_parity_claim_supported. Each gate: what it checks, how to run it, pass criteria.

**6. Resolve known gaps** G-007 through G-015 — update the relevant skills and mark each as resolved in known-gaps.md.

### Hard constraints

- **Do NOT modify hooks** (v0.6 hooks are verified and stable; v0.8 worker broke them by rewriting — don't repeat that mistake).
- **Do NOT modify the v0.7 state scripts** (they're verified and stable).
- **Only modify**: the 3 skills (verifier, reviewer, librarian), the 3 agents (same), the 4 check scripts, the 4 new docs, the known-gaps doc, and the skills with gaps (start-work, ulw-loop, ultrawork, review-work).
- **All scripts executable** (`chmod +x`).
- **Use `${CODEBUDDY_PLUGIN_ROOT}`** in all script paths.
- **Every gap fix must trace to a LazyCodex source** — cite the file path and line range.
- **Update `docs/lazyworkbuddy-parity-ledger.md`** — mark verifier/reviewer/librarian as hardened.

### Verification (perform before reporting done)

1. Run `bash lazyworkbuddy-plugin/scripts/lazyworkbuddy-plugin-doctor.sh` — must PASS.
2. Run `bash lazyworkbuddy-plugin/scripts/lazyworkbuddy-smoke-test.sh` — must PASS.
3. Run `bash lazyworkbuddy-plugin/scripts/lazyworkbuddy-verify.sh` — must run all checks and output compact JSON.
4. Run `bash lazyworkbuddy-plugin/scripts/lazyworkbuddy-security-check.sh` — must scan for secrets and report none found.
5. Run `bash lazyworkbuddy-plugin/scripts/lazyworkbuddy-docs-check.sh` — must check links and report.
6. Run `bash lazyworkbuddy-plugin/scripts/lazyworkbuddy-parity-check.sh` — must compare against LazyCodex and report.
7. Confirm hooks NOT modified: `git diff --stat lazyworkbuddy-plugin/scripts/hooks/` should be empty.
8. Confirm all 9 known gaps (G-007–G-015) are marked resolved in known-gaps.md.
9. Confirm the 12 quality gates are documented in quality-gates.md.
10. Parity ledger updated.

### Required final response format

End your work with a report containing exactly these sections:
- **Quality-control inventory** (table: component | what it does | status)
- **Gap resolution summary** (G-007 through G-015: resolved how)
- **What I inspected** (file paths read)
- **What I found** (key hardening decisions)
- **What I changed** (every file created/modified, with one-line summary)
- **How to run it** (verify + security-check + docs-check + parity-check commands)
- **Verification performed** (the 10 checks above, each PASS/FAIL)
- **Remaining gaps** (anything unresolved for v0.10)
- **Next prompt to paste** (the v0.10 delegation prompt, ready to use)
